import { DetailsTools, ListProducts } from "../../shared/components"
import { LayoutBase } from "../../shared/layouts/"

export const Dashboard = () => {

    return (
        <LayoutBase titulo="Dashboard" toolbar={
        (< DetailsTools />)}>
            Teste
        </LayoutBase>
           
    )
}